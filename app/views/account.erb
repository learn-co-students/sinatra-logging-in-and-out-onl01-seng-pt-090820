<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
      <p><% if Helpers.is_logged_in? %><p>
      <h1>"Welcome" <%= Helpers.user(session).username %></h1>
      <h3>"Your Balance:" <%= Helpers.user(session).balance %></h3>
    <input type="submit" value="logout">
  </body>
</html>